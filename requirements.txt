# LaunchDarkly — agent-graph SDK (DEVREL-741: bumped from 0.12.0)
launchdarkly-server-sdk>=9.0.0
launchdarkly-server-sdk-ai>=1.0.1
# Companion packages bind tools + models per framework (used by the LangGraph and
# OpenAI Agents runners). Strands + ADK bind with their native helpers.
launchdarkly-server-sdk-ai-langchain>=0.8.0
launchdarkly-server-sdk-ai-openai>=0.7.0

# The four orchestration frameworks (the experiment variants)
langgraph>=1.0.0
langchain>=1.0.0
langchain-anthropic>=1.0.0
strands-agents>=1.0.0   # import name is `strands`
openai-agents>=0.17.0
google-adk>=2.0.0
google-genai>=1.0.0     # `from google.genai import types` in the ADK runner
# Runs the pinned Claude model through the OpenAI Agents + Google ADK runners
litellm>=1.80.0

# Providers
anthropic>=0.40.0
openai>=1.0.0
boto3>=1.28.0          # optional — Strands via AWS Bedrock
botocore>=1.31.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
PyYAML>=6.0            # scripts/launchdarkly/bootstrap.py reads the manifest
arxiv>=2.0.0           # scripts/download_papers.py + fetch_paper tool
pypdf>=4.0             # fetch_paper tool (full-text retrieval)
PyPDF2>=3.0            # scripts/download_papers.py
